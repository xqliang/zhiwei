# umap stub：wespeaker/diar/umap_clusterer.py 顶部 `import umap`，cluster() 内用 umap.UMAP。
# diarization 路径(sidecar 不做——Go 侧聚类)，embedding 不走；空类让 import 通过即可。


class UMAP:  # noqa: D101
    def __init__(self, *_a, **_k):
        raise NotImplementedError("umap stub: diarization 未启用")
