services:
  mysql:
    image: mysql:8.4
    container_name: zhiwei-mvp-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: zhiwei
      MYSQL_PASSWORD: zhiwei
      MYSQL_DATABASE: zhiwei
    ports:
      - "3307:3306"
    volumes:
      - zhiwei-mvp-mysql-data:/var/lib/mysql
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

volumes:
  zhiwei-mvp-mysql-data:
